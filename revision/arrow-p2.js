const getInput = (num1, num2) => {
    const frist = num1 + 2;
    const second = num2 + 2;
    const gonfol = frist * second;
    return gonfol;
}
// console.log(getInput(4, 4));

const getInput1 = (num1, num2) => `${((num1 + 2) + (num2 + 2)) * 3} `;
// console.log(getInput1(4, 4));

const getinput2 = (num1, num2) => {
    const frist = num1 + 2;
    const second = num2 + 2;
    const total = frist * second;
    return total
};

console.log(getinput2(3, 3));